import request from "supertest";
import app from "../../app";

describe(
  "Task Comments",
  () => {

    it(
      "should add comment",
      async () => {

        expect(true).toBe(true);
      },
    );
  },
);