import request from "supertest";
import app from "../../app";

describe(
  "Task Time Logs",
  () => {

    it(
      "should log time",
      async () => {

        expect(true).toBe(true);
      },
    );
  },
);